# Runtime dependencies for the hierarchical online switching-observer models.
# These are the only third-party imports across the .py files in this folder;
# everything else used is the Python standard library.
#
# Setup (with uv):
#     uv venv
#     uv pip install -r requirements.txt
#     source .venv/bin/activate
# Or with plain pip:
#     python3 -m venv .venv && source .venv/bin/activate
#     pip install -r requirements.txt
#
# Lower bounds are the versions this code has been verified against; newer is fine.

numpy>=2.2
scipy>=1.16
pandas>=2.2
matplotlib>=3.9
